import './index.css'
import ReactDOM from 'react-dom'
import React from 'react'

import App from './App'

ReactDOM.render(
  <App/>,
  document.getElementById('root')
)

// COMENTARIOS:

 //vamos aprender em breve sobre JSX
 // para funcionar essa sintaxe eu preciso do import do REACT,
 // sem ele é impossivel continuar
 // Nome do import é nos quem decidimos o que colocar
 // retornamos sempre o componente como se fosse uma tag
